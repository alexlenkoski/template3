{
    // Avoid VSCode starting the '.venv' environment automatically when opening a terminal.
    // We've seen some issues with this, such as the virtual environment not being
    // activated correctly. Instead, enter the environment manually by running:
    // - `poetry shell` if using poetry, or:
    // - `source .venv/bin/activate` on Linux.
    // - `.venv/Scripts/active.ps1` on Windows.
    "python.terminal.activateEnvironment": false,

    // Debug not just your own code when debugging in interactive window:
    "jupyter.debugJustMyCode": false,

    //// Install 'Ruff' extension and uncomment below to disable
    //// Pylance linting and format with 'ruff' instead.
    // "python.analysis.ignore": [
    //     "*"
    // ],
    // "[python]": {
    //     "editor.formatOnSave": true,
    //     "editor.codeActionsOnSave": {
    //         "source.fixAll": "explicit",
    //         // To sort imports, set "source.organizeImports": "explicit":
    //         "source.organizeImports": "never"
    //     },
    //     "editor.defaultFormatter": "charliermarsh.ruff"
    // }
}
